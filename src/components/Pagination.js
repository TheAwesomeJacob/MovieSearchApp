
const Pagination = ({ totalPages }) => {
    const [currentPage, setCurrentPage] = useState(1);

    return (
        <div>
            <div>...</div>
            <div>{currentPage}</div>
            <div>...</div>
        </div>
    )
}